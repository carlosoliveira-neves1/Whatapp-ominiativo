# WhatsApp Provider
# Set to 'true' to use Z-API instead of Baileys
USE_ZAPI=false

# Z-API Configuration (required if USE_ZAPI=true)
# Get these from https://z-api.io after creating an instance
ZAPI_INSTANCE_ID=your_instance_id
ZAPI_TOKEN=your_token
ZAPI_CLIENT_TOKEN=your_client_token

# Server
PORT=3001
